var pay = $('.pay_cost');
pay.html(parseInt(pay.html()) + <%= @cost %>);

<% if @cost > 0%>
    $("#tickets").append('<%= escape_javascript(render('tickets/ticket', timetable: @timetable, place: @place)) %>');
<%end%>